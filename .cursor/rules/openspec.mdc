---
description: 自动加载仓库级 OpenSpec 与隐私规则
alwaysApply: true
---

始终遵守根目录 `AGENTS.md`。需要创建、实现或归档 OpenSpec change 时，使用 Cursor 提供的 `/opsx-propose`、`/opsx-apply`、`/opsx-archive` 原生 workflow，并严格遵守各阶段的独立授权边界。
