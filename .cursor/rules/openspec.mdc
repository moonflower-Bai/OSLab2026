---
description: 改实验代码前自动走 OpenSpec，不依赖斜杠命令
alwaysApply: true
---

改仓库里的实验代码之前，先按根目录 `AGENTS.md` 留下 OpenSpec 记录，再改代码。不要等用户输入 `/opsx-propose`。

只回答问题、不改文件时，不要创建 change。步骤以 `AGENTS.md` 和 `.agents/skills/openspec-propose/SKILL.md` 为准。仓库指令优先于技能里「未点名 OpenSpec 就跳过」的分支。
