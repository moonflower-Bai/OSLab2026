# Tasks

## 1. 约定

- [x] 1.1 重写 `prompts/README.md`，写明一组对话一个文件，以及压缩后从环境重算会话号。确认其中没有要求去列举 `prompts/`。
- [x] 1.2 把同一规则写入 `AGENTS.md` 和 `CLAUDE.md`，并确认两个文件逐字相同。
- [x] 1.3 更新 `.cursor/rules/openspec.mdc`、根目录 `README.md` 和 `openspec/config.yaml` 里的旧说法。确认根说明不再写一条提示一个文件。

## 2. 收拢已有记录

- [x] 2.1 把已拆开的单条提示并入当前 Cursor 会话文件，删除旧文件。确认会话文件里有这些原文，且路径使用 store 目录中的会话号。
